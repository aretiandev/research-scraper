## Asyncio boilerplate code

# async def main():
#     s = AsyncHTMLSession()
#     tasks = (scrape_url(s, url, items=items) for url in batch)
#     batch_result = await asyncio.gather(*tasks)
#     print(batch_result)

# asyncio.run(main())
